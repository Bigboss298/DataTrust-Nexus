{
  "name": "datatrust-nexus-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for DataTrust Nexus on BlockDAG",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network blockdag_testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network blockdag_mainnet",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4",
    "dotenv": "^16.3.1"
  }
}

